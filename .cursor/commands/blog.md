# Blog Righetto

Leggi e applica `.cursor/skills/righetto-blog/SKILL.md` + **`TEST-SKILL/skill-editoriale-visivo.md`** (comando permanente: ricerca strategica + immagini/grafiche).

**Prima di scrivere:** ricerca 7 fasi (§15) + continuità §16-TER (`build_editorial_memory.py` + `audit_editorial_continuity.py --id eq-XXX`) + `audit_editorial_research.py --id eq-XXX` + `check_doppioni_sito.py`.

**Prima di chiudere:** `audit_blog_visuals.py` + `audit_blog_publishability.py` (§18) + `audit-foto-ai.mjs` + `validate-page.js`.

Se l'utente non ha specificato il tema, proponi 2–3 angoli con fonte istituzionale (OMI, FIMAA, ISTAT, ADE) e attendi scelta.

Fine task: `validate-page.js`, sitemap se nuova URL, commit in italiano. Push se l'utente lo chiede.
