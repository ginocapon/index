# Fix mobile Righetto

Leggi e applica `.cursor/skills/righetto-fix-mobile/SKILL.md`.

Focus: pagina indicata dall'utente, viewport 375px (iPhone) e 768px (tablet).

**Hero landing:** se testo bianco illeggibile su iPhone → split foto + pannello scuro sotto (lezione in skill § «hero landing su iPhone»).

Modifica minima — solo CSS/HTML necessari. Bump `?v=N` su asset toccati.

Fine task: commit + push in italiano se ci sono modifiche **e** l'utente ha chiesto push.
